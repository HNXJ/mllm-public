# Developer Plan

## Section 1: Brainstorm
Roadmap: 1. VLM Caching: Save figure crops and generated visual descriptions in `.cache/deepread/` mapped by the PDF MD5 hash. [COMPLETED] 2. Robust JSON Extractor: Build a helper using regex and curly brace count parsing to locate and cleanly parse JSON output blocks from LLM responses even with markdown wrapping. [COMPLETED] 3. Smart Prompt Compression: If the prompt exceeds the model's context-window limit, dynamically compress the least critical sections (like Methods) while keeping Abstract/Results/Glossary intact. [COMPLETED] 5. Pre-flight checks: Add a --preflight CLI switch to query server readiness, list loaded models, and verify local resources before starting evaluation runs. [COMPLETED]

## Section 2: Execution Plan Table

File | Purpose | Score | Actions | Rules | Edits | Notes | Cautions
--- | --- | --- | --- | --- | --- | --- | ---
src/jmllm/util/helpers.py | Support helpers, text cleaners, JSON parsing, MD5 hashing, and prompt compression logic. | 100/100 | 1. Add extract_json_block() to robustly extract and parse nested JSON string blocks from markdown tags. 2. Implement calculate_pdf_hash() for cache lookups. 3. Add compress_prompt() to truncate/summarize methods if token limit is exceeded. | Must remain stateless and robust. | Completed: JSON extraction, PDF hashing, and prompt compression helpers. | Optimal token management and parsing helpers. | None
src/jmllm/pipeline/loaders.py | PDF layouter and DeepRead extraction loader integrating visual detection and VLM caching. | 100/100 | 1. Integrate MD5 caching checks: check if visual descriptions exist under .cache/deepread/{pdf_hash}/ before calling VLM. 2. Save generated crops and descriptions to cache directory on success. | Avoid redundant VLM completions calls. | Completed: DeepReadLoader cache lookup and save logic. | Reduces extraction time for cached files to near-zero. | None
src/jmllm/pipeline/controller.py | Core orchestrator running sequential DeepRead and parallel ThreadPoolExecutor evaluations. | 100/100 | 1. Implement preflight check routines: ping server, check active models, verify inputs/glossary. 2. Add context-length check on prompt before LLM invocation, calling helper's prompt compressor if limit is exceeded. | Do not execute evaluations if preflight checks fail. | Completed: Preflight checks and token length validation controls. | Ensures prompt size is within model's context limit. | None
src/jmllm/pipeline/cli.py | CLI interface options parsing. | 100/100 | Add --preflight boolean flag parameter option. | Follow standard argparse convention. | Completed: add --preflight parameter flag. | Enables sanity check checks CLI execution. | None

# DEPRECATED: Parsing logic has been migrated or replaced by canonical implementations in src/mllm/data/.
# This file is retained for legacy compatibility and will be removed in a future release.

import logging
from pathlib import Path
from typing import List, Dict, Any, Optional

logger = logging.getLogger("mllm-parsing")

# NOTE: The functions extract_python_vars, clean_json_string, and aggregate_json_results
# have been migrated or replaced by more robust implementations in src/mllm/data/preprocessors.py.
# This file is retained for backward compatibility. Ensure code is updated to use
# the canonical functions in src/mllm/data/preprocessors.py.

# If specific backward compatibility wrappers are needed, they can be added here.
# For now, we assume the removal of the actual function code.
